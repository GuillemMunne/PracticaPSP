<UserControl x:Class="PokedexWpf.Controls.DamageModifierBlockControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:PokedexWpf.Models">
  <Border Background="#FF37474F" CornerRadius="10" Padding="10" Margin="0,4">
    <StackPanel>
      <TextBlock Text="{Binding ModifierLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="Bold" Foreground="White" />
      <ItemsControl ItemsSource="{Binding Types, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate DataType="{x:Type models:PokemonType}">
            <Border Background="#FF607D8B" CornerRadius="8" Padding="6,2" Margin="4,2,4,0">
              <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" />
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </Border>
</UserControl>
